# NDDev Validate

Use `skills/nddev-builder/SKILL.md`, then read
`skills/nddev-builder/references/validation-release.md`. Run only public,
module-local validators and non-live temporary-target smoke checks unless the
owning phase explicitly authorizes private lanes.
