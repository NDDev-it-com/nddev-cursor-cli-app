# NDDev Lifecycle

Use `skills/nddev-builder/SKILL.md`, then read
`skills/nddev-builder/references/installation-lifecycle.md`. Check target-owned
software install, setup install/switch/migrate, launch, restore, and remove
behavior without touching live user configuration.
