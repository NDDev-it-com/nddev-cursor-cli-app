# nddev-builder Cursor plugin

This plugin is projected into the selected Cursor configuration target as a
local native Cursor plugin. It contains only public guidance artifacts: a rule,
a skill, and a custom agent. The setup manager does not create or mutate Cursor
team marketplaces.
